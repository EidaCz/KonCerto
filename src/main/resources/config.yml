# Messages translation
lang: en

music:
  # Allow playing the same music from multiple sources
  overlapping: false

# Player resourcepack
resourcepack:
  # Kick player without downloaded resourcepack
  player-force-kick: true
  # Ignore failed download
  player-ignore-if-failed: false
  # Allow only records supported by server resource pack
  records-supported-only: true